const routes = {
    notFoundPage: (): string => "*",
    mainPage: (): string => "/",
    login: (): string => "/login",
    signUp: (): string => "/signup",
    game: (): string => "/game",
};

export default routes;
